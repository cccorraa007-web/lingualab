# 西语「输入→输出」训练网站 · 项目计划大纲

## 一、项目定位

一个面向**中国高校西语学习者**的 Web 响应式平台，解决核心痛点：**「能做题，不能交流」**。

核心价值主张一句话：**把用户读过的、听过的材料，自动转化成一套「能说会用」的语料库，并围绕它做对话、润色、跟读训练，实现从被动输入到主动输出的转化。**

---

## 二、目标用户与痛点

| 用户画像 | 痛点 | 本产品解法 |
|---------|------|-----------|
| 大二~大四西语专业生 | 语法/阅读强，听说弱 | 语料库 + 对话练习强制输出 |
| 备考 DELE/SIELE 学生 | 口语题没素材、不地道 | 题库 + 热点 + 润色 |
| 准备出国交换/留学 | 怕听不懂当地口音俗语 | 方言俗语库 + 影子跟读 |
| 自学者 | 缺体系、缺反馈 | AI 评分润色闭环 |

---

## 三、核心功能模块拆解

### 模块 1：语料库（Corpus）—— 数据底座
这是整个产品的地基，其他模块都依赖它。

**输入来源**
- 文字：粘贴文章、导入 URL、上传 PDF/Word
- 音频：上传播客/音频、贴 YouTube 链接（转文字）

**AI 处理流水线（Pipeline）**
```
原始材料 → 文本提取(音频走ASR转写) → 话题自动分类 → 提取关键词 → 提取地道表达/固定搭配 → 按CEFR标难度 → 存入语料库
```

**产出**
- 按话题分类的语料卡片（如：教育、旅行、职场、环保、社会热点…）
- 每张卡片含：关键词、常见表达、句型、例句、来源、难度
- 用户个人的「话题覆盖度」仪表盘（哪个话题积累多/少）

### 模块 2：口语对话练习（输入→输出转化）
- **AI 选题**：从用户语料库中选话题 + 参考 DELE/SIELE 口语真题格式 + 结合当下热点生成题目
- **对话模式**：AI 当考官/语伴，模拟 SIELE T2（看图描述）、T3（情景对话）、T4（观点表达）、T5（辩论）
- **难度自适应**：根据用户表现动态调难度
- **评分反馈**：从连贯性、语法、词汇、流利度四维度打分（对齐 SIELE 评分标准）

### 模块 3：AI 润色（输出地道性保障）
- **文字输入润色**：用户写西语 → AI 改写成地道表达 → 显示「原文 vs 修改」对比 + 错误原因说明
- **语音输入润色**：录音 → ASR 转文字 → 润色 → 高亮错误 + 给出更地道的说法
- **关键设计**：不只纠错，还要告诉用户「为什么这么说更地道」，并链接回语料库中的对应表达

### 模块 4：方言 / 俗语学习
- 按国家地区标记：西班牙（castellano）、墨西哥、阿根廷（rioplatense）、哥伦比亚、智利等
- 同一意思的「多地区说法」对照（如「汽车」coche/carro/auto）
- 俗语卡片：含义 + 使用场景 + 例句 + 地区标签
- 与语料库联动：语料中出现的俗语自动标记地区

### 模块 5：影子跟读（Shadowing）
- 音频播放器：逐句切分、单句循环、慢速/原速切换
- 跟读录音 → 与原文对比（发音、语调、语速）
- 素材优先从用户自己的语料库音频中选取（强化输入→输出闭环）

---

## 四、技术架构

```
前端 (响应式)         后端 / API             AI 服务              数据
┌─────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────┐
│ Next.js      │──▶│ API 路由 /    │──▶│ 大模型 API    │   │ PostgreSQL│
│ React        │   │ Serverless   │   │ (Claude/GPT) │   │ + 向量库  │
│ Tailwind     │   │ 函数          │   │ ASR 语音识别  │   │ (pgvector)│
└─────────────┘   └──────────────┘   │ TTS 语音合成  │   └──────────┘
                                     └──────────────┘
```

- 响应式：Tailwind 移动优先，手机/平板/桌面全适配
- 音频：浏览器 MediaRecorder 录音 + 上传
- 语音：ASR（如 Whisper）、TTS 用于 AI 对话语音

---

## 五、核心数据模型（简版）

```
User（用户）
  ├─ Material（阅读/听力材料）
  ├─ CorpusCard（语料卡片：话题、关键词、表达、难度、来源）
  ├─ Topic（话题分类）
  ├─ PracticeSession（练习记录）
  ├─ PolishRecord（润色记录：原文/修改/说明）
  ├─ SlangEntry（俗语条目：地区、含义、例句）
  └─ ShadowingRecord（跟读记录）
```

---

## 六、开发里程碑（适合 vibe coding 迭代）

| 阶段 | 目标 | 交付物 | 说明 |
|------|------|--------|------|
| **M1 骨架** | 能跑起来 | 响应式首页 + 导航 + 布局 | 先有壳 |
| **M2 语料库核心** | 数据闭环 | 材料导入 → AI 分类提取 → 语料卡片展示 | 最重要，先做通 |
| **M3 润色** | 输出反馈 | 文字润色（先不做语音） | 最简单、见效快 |
| **M4 口语练习** | 对话闭环 | AI 选题 + 对话 + 四维评分 | 核心价值 |
| **M5 语音能力** | 听说打通 | 语音输入润色 + 录音 | 加 ASR |
| **M6 方言俗语** | 差异化 | 俗语库 + 地区标签 | 特色功能 |
| **M7 影子跟读** | 精听练 | 逐句播放 + 跟读对比 | 收尾 |
| **M8 打磨上线** | 可用 | 进度系统、部署到 Vercel | 发布 |

> 关键原则：**每个里程碑都能单独运行、单独体验**，不要一口气做完全部再测试。

---

## 七、技术栈推荐

- **前端**：Next.js + React + Tailwind CSS
- **后端/API**：Next.js API Routes（省去单独后端）
- **数据库**：Supabase（云端 Postgres + pgvector + 认证 Auth + 存储，一体化）
- **AI**：OpenAI 或 Claude API（分类、提取、润色、对话、评分）
- **语音**：Whisper（ASR）+ 任意 TTS
- **部署**：Vercel（托管 Next.js）

---

## 八、风险与挑战

| 风险 | 应对 |
|------|------|
| AI 分类/提取质量不稳定 | 用结构化输出（JSON Schema）约束 AI，加人工可编辑 |
| 西语 ASR 准确性（尤其口音） | 用支持西语的 Whisper 模型，允许用户手动校正 |
| AI 评分不权威 | 对齐官方 CEFR 评分标准做 prompt 设计，声明「仅供参考」 |
| 版权问题（导入材料） | 优先用户自己上传/粘贴的内容 |
| 成本（AI 调用频繁） | 做用量限制、缓存结果 |

---

## 九、MVP 最小可行版本建议

如果只想快速验证，第一版只做 **3 件事**：

1. **语料库**：粘贴一篇文章 → AI 分类 + 提取表达 → 展示卡片
2. **文字润色**：写一段西语 → AI 润色 + 对比展示
3. **简单对话练习**：AI 选话题出题 → 用户文字作答 → AI 评分

这三件事验证核心假设：「用户愿意靠 AI 把读过的变成能说的」。

---

## 十、部署、认证与付费

### 1. 总体方案

从本地开发平滑演进到线上多用户产品，无需中途大改架构：

- **部署**：Vercel（git push 自动部署）
- **数据库 + 认证**：Supabase（云端 Postgres + pgvector + Auth + 存储，一体化）
- **付费**：Stripe（订阅制）

### 2. 部署（Vercel）

- Next.js 官方托管，连接 GitHub 仓库后 push 即自动部署，有免费档
- 环境变量（Supabase、AI、Stripe 密钥）在 Vercel 面板配置，不写进代码

### 3. 认证与数据隔离（Supabase Auth + RLS）

- 注册/登录：邮箱 + 第三方登录（GitHub / Google）
- **多租户隔离**：所有数据表已预留 `user_id` 字段，配合 Supabase RLS（行级安全）策略，每个用户只能读写自己的数据
- API 路由通过 Supabase 校验登录态后，才能执行语料处理、润色等操作

### 4. 付费模型（Stripe）

- **免费档**：每月处理 N 篇材料 + 有限次数口语练习
- **付费档**：无限材料处理 + AI 口语练习 + 语音功能
- 通过 Stripe 订阅实现，用 Supabase 存订阅状态

> 注意（国内支付）：Stripe 需海外主体/银行账户；微信/支付宝需企业资质。
> 过渡方案：先做「免费 + 会员邀请码」手动开通，等具备资质再接支付。

### 5. 演进时间线

| 阶段 | 接入内容 |
|------|---------|
| M2 | 接 Supabase 数据库（本地开发也连云端，不上 SQLite） |
| M3 | 接 Supabase Auth + RLS 数据隔离 |
| M8 | 接 Stripe 订阅付费 |

---

# 模块 1「语料库」细化设计（AI 处理细节）

## 1. 功能目标

用户把读过的文章/听过的音频丢进来，系统自动产出一个**按话题组织、可直接用于口语练习的语料库**。核心要解决的问题：

> 用户读了一堆东西，但不知道怎么「用」它们开口说话。

所以语料库的关键产出不是「收藏夹」，而是**「会说的话」**——关键词、地道表达、句型、可套用例句。

---

## 2. 用户操作流程

```
用户 → 选择输入方式 → 系统处理 → 展示语料卡片 → 用户可编辑/收藏 → 用于口语练习
```

**输入方式（3 种）**
1. 粘贴文本（最快，M2 先做这个）
2. 导入 URL（抓取正文）
3. 上传音频/视频 → ASR 转写（M5 再做）

处理进度用「后台任务 + 进度条」展示，因为 AI 处理要几秒到几十秒。

---

## 3. AI 处理流水线（Pipeline）详细设计

每一步都调用大模型，用**结构化输出（JSON Schema）**约束返回格式，避免 AI 乱写。

### 步骤 0：文本提取 & 清洗
- URL：抓正文、去广告/导航（可用 `trafilatura` 或抓取库）
- 音频：Whisper 转写，输出带时间戳的文本
- 统一清洗：去 HTML、分句、按段落切块（长文切成 500-800 词的块再处理）

### 步骤 1：话题分类
**输入**：一段清洗后的文本
**话题分类体系 —— 有限标签 + 自动合并设计**

三条核心原则：
1. **标签有限**：正式标签（可筛选）只有固定的 12 个一级主题，用 JSON Schema 的 `enum` 约束，AI 只能从中选，**禁止自创标签**。
2. **相近话题自动合并**：细粒度子话题（书店、电影、音乐节…）不作为正式标签，AI 先识别出子话题，再归并到最接近的一级主题。
3. **粒度适中**：主题既不能太细（每个具体活动一个标签），也不能太宽泛。像「日常生活」「生活场景」这类覆盖过广的标签**禁止出现**，必须拆解到具体主题。

**12 个一级主题（正式标签，enum 取值）**

| slug | 中文 | 归入的子话题（示例） |
|------|------|---------------------|
| vida-cultural | 文化生活 | 书店、电影、音乐节、文学、艺术、博物馆、演出、时尚、节日 |
| educacion | 教育学习 | 学校、课程、考试、留学、教学、语言学习、读书方法 |
| trabajo | 工作职场 | 求职、面试、创业、薪酬、远程办公、职业规划、同事关系 |
| tecnologia | 科技与互联网 | AI、手机、网络、数码产品、数据隐私、科技创新 |
| salud | 健康与医疗 | 医疗、心理健康、疾病、公共卫生、养生、营养 |
| medio-ambiente | 环境与气候 | 气候变化、污染、能源、环保、自然灾害、动物保护 |
| sociedad | 社会与热点 | 时事新闻、移民、平等、老龄化、法律、城市治理 |
| economia | 经济与消费 | 消费、物价、购物、理财、经济政策、通胀、房价 |
| viajes | 旅行与交通 | 旅游、交通、住宿、出行方式、景点、跨文化体验 |
| relaciones | 情感与家庭 | 爱情、友情、家庭、婚姻、育儿、人际沟通 |
| gastronomia | 美食与餐饮 | 餐厅、烹饪、食材、饮食文化、咖啡茶饮 |
| deporte | 体育与休闲 | 体育赛事、运动项目、健身、运动员、户外活动 |

**边界规则（避免重叠 / 过宽）**

- 健身、运动行为 → `deporte`；但「健康饮食」「减脂营养」→ `salud`（按内容侧重判断）
- 「旅行途中的美食」→ 按主要篇幅判断，二选一作 primary，另一个放 secondary
- 「手机购物」→ 侧重消费行为归 `economia`，侧重科技产品归 `tecnologia`
- 禁止宽泛标签：日常生活、生活场景、综合、其他（覆盖过广，强制拆解）

**自动合并机制**

- 一级主题固定 12 个，`primary_topic` / `secondary_topics` 必须来自这 12 个 enum 值
- `subtopics`（细粒度子话题）是自由文本，用于卡片描述，**不参与筛选**，因此新增子话题（如「播客」「剧本杀」）无需新建一级主题，标签集合保持稳定
- `secondary_topics` 最多 2 个

**输出（JSON）**：
```json
{
  "primary_topic": "vida-cultural",
  "secondary_topics": ["sociedad"],
  "subtopics": ["cine", "festivales de música"],
  "cefr_level": "B1"
}
```

### 步骤 2：关键词提取
**输入**：文本 + 话题
**输出**：15-25 个关键词，每个带词性和中文释义
```json
{
  "keywords": [
    {"word": "brecha digital", "pos": "n.", "zh": "数字鸿沟", "freq": 3}
  ]
}
```

### 步骤 3：地道表达提取（本模块的灵魂）
这一步决定「语料库能不能用来说话」。要提取的不是单词，而是**能直接套用的搭配/固定表达**。

**输出**：
```json
{
  "expressions": [
    {
      "phrase": "estar al día de",
      "zh": "了解…的最新情况",
      "type": "colocacion",
      "register": "neutral",
      "example": "Estoy al día de las noticias.",
      "example_zh": "我了解最新新闻。"
    }
  ]
}
```

**提取重点**（prompt 里要明确要求 AI 找这几类）：
- 固定搭配（colocaciones）：`tener en cuenta`、`dar lugar a`
- 连接词/篇章标记：`por un lado... por otro lado`、`cabe destacar`
- 口语高频短句：`¿Me explico?`、`te cuento`
- 有西语特色的表达：`costar un ojo de la cara`

### 步骤 4：生成「口语练习素材」
这是**输入→输出转化**的关键桥梁。AI 基于语料，直接生成可用于对话的句子/问题：
```json
{
  "speaking_prompts": [
    {
      "question": "¿Crees que la tecnología aumenta la desigualdad?",
      "useful_chunks": ["aumentar la brecha", "estar al día de"],
      "sample_answer_hint": "Por un lado... por otro lado..."
    }
  ]
}
```

---

## 4. 语料卡片（最终展示的数据结构）

```json
{
  "id": "card_001",
  "source_material_id": "mat_001",
  "source_type": "url",
  "title": "La brecha digital en América Latina",
  "primary_topic": "tecnologia",
  "secondary_topics": ["sociedad"],
  "subtopics": ["brecha digital", "internet rural"],
  "cefr_level": "B2",
  "keywords": [],
  "expressions": [],
  "speaking_prompts": [],
  "original_snippet": "原文片段...",
  "created_at": "..."
}
```

前端展示：一张「语料卡片」= 话题标签 + 关键词云 + 表达列表（可点开看例句）+ 口语练习入口按钮。

---

## 5. 数据库表结构

```
materials          -- 原始材料（文本/URL/音频元数据 + 转写全文）
  id, user_id, type, title, raw_text, created_at

corpus_cards       -- 语料卡片（一次处理生成）
  id, material_id, primary_topic(enum), secondary_topics(数组), subtopics(自由文本), cefr_level, ai_output(JSON), created_at

keywords           -- 关键词（拆表便于检索）
  id, card_id, word, pos, zh, freq

expressions        -- 地道表达
  id, card_id, phrase, zh, type, register, example, example_zh

speaking_prompts   -- 口语练习素材
  id, card_id, question, useful_chunks, sample_hint

topics             -- 话题字典（固定 12 个一级主题，受控词表）
  id, slug(唯一), name_zh, name_es, is_active
```

> 用 `pgvector` 给 `expressions.phrase` 和 `materials.raw_text` 建向量索引，后续做「语义检索」（比如练习时检索相关表达）。

---

## 6. API 设计

```
POST /api/materials          # 创建材料（粘贴文本/提交URL）
POST /api/materials/{id}/process   # 触发AI处理流水线（异步）
GET  /api/materials/{id}/status    # 查询处理进度
GET  /api/corpus?topic=tecnologia  # 按话题查语料卡片
PATCH /api/cards/{id}        # 用户手动编辑卡片
DELETE /api/cards/{id}       # 删除卡片
```

---

## 7. 关键实现要点

| 要点 | 方案 |
|------|------|
| AI 输出不稳定 | 强制 JSON Schema + 失败重试 + 校验字段 |
| 长文处理 | 分块处理再合并去重 |
| 成本控制 | 缓存已处理的材料结果；同类请求去重 |
| 中文释义准确性 | 让 AI 同时输出西语+中文，用户可手动改 |
| 话题分类不准 | 允许用户手动改话题标签，反馈回训练 |
| 表达重复 | 同一材料内去重；跨材料不强制去重（可反映真实频率） |

---

## 8. M2 里程碑的验收标准

完成以下就算「语料库核心」通了：
- [ ] 粘贴一篇西语文章 → 处理成功 → 看到分类、关键词、表达、口语练习素材
- [ ] 语料卡片能按话题筛选
- [ ] 用户能手动编辑/删除卡片
- [ ] 处理失败有友好提示，可重试

---

# 模块 2「口语对话练习」细化设计

## 1. 功能目标

模拟 DELE/SIELE 口语题，让用户把语料库里的积累「说出来」，并在每次练习后沉淀用户的**个性化错误档案**，下次练习**刻意针对这些易错点**，形成「发现错误 → 记忆 → 复练 → 纠正」闭环。

## 2. 核心流程

```
选题(AI) → 出题 → 用户作答(文字/语音) → AI 四维评分 + 润色 → 提取新错误 → 更新错误档案
                                                                    ↓
                    下次选题时从错误档案抽取易错点 → 注入题目/对话引导 → 若再错即时纠正
```

## 3. 选题与出题

- **话题来源**：从用户语料库积累较多的话题里选（可输出，也有话可说），并混入现实热点
- **题型对齐 SIELE/DELE**：T2 看图描述、T3 情景对话、T4 观点表达、T5 辩论
- **难度自适应**：根据历史评分调整 CEFR 难度

## 4. 错误档案（本模块的灵魂，解决「中式西语反复犯」）

练习结束后，AI 从用户作答中提取**不地道的表达 / 中式西语 / 高频语法错误**，存入档案：

| 字段 | 说明 |
|------|------|
| error_type | 错误类型：语法 / 中式西语(直译) / 词汇误用 / 语域不当 / 搭配错误 |
| wrong | 用户原话（错误表达） |
| correct | 正确/地道说法 |
| reason | 为什么这么说不对（中文解释） |
| topic | 所属话题 |
| count | 累计出错次数 |
| mastery | 掌握度（0-1，越低越优先复练） |

**错误类型示例（重点抓「中式西语」）**
- 直译错误：`te acuerdo`（想说"我同意你"）→ `estoy de acuerdo contigo`
- 语域不当：正式作答里用 `bueno, pues...` 太口语
- 搭配错误：`hacer una pregunta` ✓ vs `preguntar una pregunta` ✗

## 5. 刻意练习机制（间隔重复思想）

- 下次练习时，按「掌握度低 + 出错次数多」排序抽取易错点
- **注入方式**：题目明确要求用到某表达（如「请用 `estoy de acuerdo con` 表达观点」），或在对话中 AI 故意引导用户使用易错表达
- 若用户再次出错 → 即时高亮纠正 + `count+1`；若连续用对 → `mastery` 提升
- 定期推送「易错点回顾卡片」

## 6. 四维评分（对齐 SIELE/CEFR）

连贯性、语法准确度、词汇丰富度、流利度，每项 0-5 分 + 综合等级（A1-C1），并声明「AI 参考评分」。

## 7. 数据模型（新增）

```
practice_sessions  -- 练习会话
  id, user_id, topic, task_type, cefr_level, score(JSON), created_at

user_errors        -- 错误档案
  id, user_id, error_type, wrong, correct, reason, topic, count, mastery, last_practiced_at
```

## 8. 验收标准（M4）
- [ ] 能选话题、出题、用户作答、四维评分
- [ ] 练习后自动提取错误并存入档案
- [ ] 下次练习能针对历史易错点出题/引导
- [ ] 再错即时纠正、用对则降低优先级

---

# 模块 3「AI 润色」细化设计

## 1. 功能目标

把用户写的/说的西语改「地道」，但**改写必须有真实语料依据，不能 AI 瞎编**，并给出理由、区分场景语气。

## 2. 核心流程

```
用户输入(文字/语音→ASR) → 选场景(语气) → 检索语料库相关表达 → AI 润色 → 展示对比+理由+来源 → 用户可收藏
```

## 3. 场景化语气（关键，避免「一刀切」）

用户先选目标场景，润色标准随之改变：

| 场景 | 语域 | 示例 |
|------|------|------|
| 商务往来 | formal | 邮件、谈判、正式汇报 |
| 学术写作 | formal | 论文、报告 |
| 朋友日常 | informal/coloquial | 聊天、WhatsApp、口头交流 |
| 半正式 | neutral | 给老师/同事的邮件 |

- 同一个意思在不同场景给出不同版本（如「再见」：`Atentamente` / `Un saludo` / `Nos vemos`）
- 语域不当是润色的重点纠错对象之一（正式场景用了口语词）

## 4. 基于真实语料（关键约束）

- **禁止 AI 瞎编表达**：改写建议优先从用户语料库 + 内置参考语料中检索，能追溯到真实来源
- **引用溯源**：每条修改点标注来源（「来自你的语料卡片 X」或「来自内置参考语料」）
- 若某处修改无法在真实语料中找到支撑，标记为「AI 建议，仅供参考」，与有依据的区分显示

## 5. 润色输出结构

```json
{
  "scenario": "business",
  "polished_text": "修改后的完整文本",
  "changes": [
    {
      "original": "te acuerdo",
      "revised": "estoy de acuerdo contigo",
      "reason": "acuerdo 是名词，不是及物动词，不能说 'te acuerdo'",
      "error_type": "gramatica",
      "source": "corpus_card_001"   // 或 "ai_suggestion"
    }
  ]
}
```

**理由必须给出**：每条修改说明「错在哪 + 为什么这么改」，用户才能举一反三。

## 6. 语音输入润色

录音 → ASR 转写 → 同流程润色。展示：转写原文（可修正 ASR 错误）→ 润色对比。

## 7. 数据模型（新增）

```
polish_records     -- 润色记录
  id, user_id, scenario, original_text, polished_text, changes(JSON), created_at
```

## 8. 验收标准（M3）
- [ ] 文字润色能跑通，展示原文/修改/理由三栏对比
- [ ] 场景可选，语气随场景变化
- [ ] 有真实语料依据的改写显示来源，无依据的标记「仅供参考」

---

# 模块 4「方言 / 俗语」细化设计

## 1. 功能目标

帮助用户应对不同西语国家的口语差异。**核心原则：地区信息在语料输入时就标注，贯穿后续所有功能。**

## 2. 地区标注（输入时完成）

- **材料导入时增加「来源地区」字段**：用户手动选择，或 AI 从文本特征自动识别
- AI 识别依据：`vos`/`vosotros` 使用、`coche`/`carro`/`auto`、`tú` vs `vos`、特定俗语等
- 地区枚举：
```
España (castellano) | México | Argentina (rioplatense) | Colombia | Chile | Perú | Neutral(通用)
```

## 3. 语料与地区联动

- 语料卡片继承来源地区标签
- 提取出的表达/俗语自动带上地区（同一表达可标注多地区变体）
- 润色/口语练习时，可按用户目标地区调整建议（如用户要去阿根廷，提示 rioplatense 说法）

## 4. 俗语库

```json
{
  "phrase": "costar un ojo de la cara",
  "zh": "贵得要命",
  "region": ["España", "neutral"],
  "register": "informal",
  "example": "Ese coche cuesta un ojo de la cara."
}
```

**多地区对照**：同一意思不同说法并列展示（「汽车」→ España `coche` / México `carro` / Argentina `auto`）。

## 5. 数据模型（新增）

```
slang_entries      -- 俗语条目
  id, phrase, zh, region(数组), register, example, example_zh, source_card_id
```

## 6. 验收标准（M6）
- [ ] 导入材料可标注来源地区
- [ ] 语料卡片/表达带地区标签
- [ ] 俗语库支持按地区筛选和多地区对照展示

---

# 模块 5「影子跟读」细化设计

## 1. 功能目标

通过跟读模仿，练发音、语调、语速和流利度。**核心能力：变速播放 + 话题分类 + 长音频自动切片。**

## 2. 自动切片（长资料 → 1 分钟片段）

- 长音频（>1.5 分钟）自动切成 **1 分钟左右**的跟读片段
- **切法**：基于 ASR 转写的时间戳，在**句子边界**切分，保证每段语义完整（不在句中硬切）
- 每段对应一个跟读练习单元，可单独循环

## 3. 变速播放

- 倍速：0.5x / 0.75x / 1x / 1.25x
- 用 Web Audio API `playbackRate` 实现，**不变调**（或用 TTS 预生成慢速版）
- 慢速用于逐句精听，原速用于最终跟读检验

## 4. 话题分类

- 跟读素材复用**语料库的话题标签**，可按话题筛选跟读内容
- 优先推荐用户自己导入的音频（已转写 + 已分类 + 已切片），强化输入→输出闭环

## 5. 跟读流程

```
播放一句(可变速) → 暂停 → 用户跟读录音 → 与原文对比(发音/流利度打分) → 下一句
```

对比维度：发音准确度、语调、语速、停顿位置。

## 6. 数据模型（新增）

```
shadowing_clips    -- 跟读片段
  id, source_material_id, topic, start_ts, end_ts, transcript, duration, order_index

shadowing_records  -- 跟读记录
  id, user_id, clip_id, audio_url, score(JSON), created_at
```

## 7. 验收标准（M7）
- [ ] 长音频能切成约 1 分钟片段，且不截断句子
- [ ] 支持 0.5x-1.25x 变速
- [ ] 跟读片段可按话题筛选
- [ ] 跟读录音能与原文对比打分
