# LinguaLab 开发日志

> 一个面向中文母语学习者的外语「输入 → 输出」训练平台。本文档记录项目从自学模式到教学模式的设计演进、采用的技术方法，以及解决的关键问题，供项目报告参考。

---

## 一、项目概述

LinguaLab 的核心价值主张：**把用户读过的材料，自动转化成一套「能说会用」的语料库，并围绕它做口语对话、写作润色、错题复习等输出训练**，解决外语学习者「会做题、不会交流」的痛点。

产品分两大模式：

- **自学模式**：面向个人自学者，包含语料库、口语练习、写作润色、错题本。
- **教学模式**：面向真实课堂，教师 + 学生双角色、班级管理、必读文章与口语训练（逐步建设中）。

学习目标语言支持**西班牙语**和**英语**，界面为中文。产品中性名 **LinguaLab**，按学习目标语言在主页显示 **HablaYa**（西语）/ **SpeakUp**（英语）。

### 当前进度一览

**已完成**：
- 自学模式：语料库、口语练习（自由练习 + 考题模式）、错题本（口译练习）。
- 教学模式：角色与班级/成员管理、必读文章、阅读勾画与批注。
- 基础设施：多用户登录与数据隔离、双语学习（西语/英语）、实时语音识别（ASR）+ 语音合成（TTS）、许可证检测、内网穿透部署。

**待开发**：
- 写作润色（自学模式）。
- 错题本「笔译」练习（自学模式）。
- 教学模式「笔头作业」板块。
- 教学模式「口语题 + 学生档案 + 班级分析 + 排行榜」。
- 微信小程序（暂缓）。

---

## 二、技术架构与技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Next.js 16（App Router）+ React 19 + Tailwind CSS | 响应式 SPA |
| 后端 | Next.js API Routes（Serverless 函数） | 省去独立后端 |
| 数据库 | Supabase（云 PostgreSQL）+ Auth 认证 + RLS 行级安全 | 一体化 |
| 大模型 | DeepSeek（deepseek-chat） | 语料分析、对话、润色、评分 |
| 语音 | 阿里云智能语音交互 NLS | ASR 实时语音识别 + TTS 语音合成 |
| 部署 | 内网穿透（cpolar） | 国内测试；Vercel 为后续正式上线备选 |

**数据流**：前端（客户端组件）通过 `/api/*` 路由调用后端；后端校验 JWT 身份后访问 Supabase 与 AI/语音服务。

---

## 三、自学模式设计

### 3.1 语料库（数据底座）

**设计**：用户粘贴一篇外语文章，AI 自动完成「话题分类 → 提取关键词/地道表达/口语题 → CEFR 难度标注」，产出语料卡片。

**技术方法**：
- 用 `processCorpus` 以结构化 JSON（`chatJSON` + `response_format: json_object`）约束大模型输出，保证结果可解析。
- 话题标签用**受控词表**（`topics.ts` 中 30 个主题）约束，并做 `resolveTopicSlug` 归一化：AI 无论输出 slug、中文名还是外语名，都归一为合法 slug，前端再映射中文显示（解决「标签出现外语」问题）。
- 语料条目分三类：`keyword`（关键词）、`expression`（地道表达）、`prompt`（口语练习问题）。
- 支持原文高亮、句子批注（`annotations` 表）、全文翻译（`translateText`）。

### 3.2 口语练习

**设计**：分「自由练习」和「考题模式」。
- 自由练习：AI 扮演考官，基于用户语料库内容由浅入深对话引导开口。
- 考题模式：参考 SIELE 题型（简单问答/情景对话/观点表达）随机抽题、限时作答。

**技术方法**：
- 语音采用**实时语音识别（WebSocket 流式 ASR）**：前端录音经 `AudioContext` 采集成 16kHz PCM，通过 WebSocket 直连阿里云 NLS，边说话边出字，结束后拿到完整转写。
- TTS 合成考官提问语音，实现「听题 → 说话 → 评价」的闭环。
- 对话结束调用 `polishAnswers` 生成逐条润色建议（原文 → 修改 → 中文原因 → 参考例句 → 错误类型）。

### 3.3 写作润色（待开发）

**设计**：用户提供作文题目、粘贴作文、附评分标准（可选），AI 生成语法纠错与分维度评分反馈；纠错建议可勾选加入错题本（复用口语润色的「建议 → 勾选 → 进错题本」闭环）。当前为占位页，尚未实现。

### 3.4 错题本

**设计**：把润色/练习中发现的错误沉淀为错题，配合练习复习：AI 出中文提示 → 学生翻译成外语 → AI 判定是否正确、连续答对自动移除。

- **口译练习（已实现）**：中文短句 → 口译成外语。
- **笔译练习（待开发）**：中文长句（更长更难）→ 笔译成外语，判题重点评价句式选择与用词准确性（词性、词义准确生动）。

---

## 四、教学模式设计（建设中）

### 4.1 角色与权限

- 账号本身**不区分身份**（注册时不选教师/学生，自学模式对两者一致）。
- 身份在**创建/加入班级时自行选择**：创建班级时选「以教师身份/以学生身份」，加入班级时同样选「以教师身份/以学生身份」——学生也可以建班，教师通过加入时选教师身份进入，支持一名以上教师。
- 班级内角色分三档：`teacher`（教师）、`leader`（班委，由教师指定）、`student`（普通学生）；教师可把普通学生设为班委或教师。

### 4.2 班级与成员管理

- **建班**：任何人可创建班级（并选择自己的身份），系统生成 6 位邀请码。
- **加入**：任何人凭邀请码申请加入，加入时选择「以教师身份/以学生身份」。
- **审批**：教师/班委审批申请。
- **班委/教师**：教师可指定普通学生为班委，或设为教师（实现多教师）。

**数据模型**：`classrooms`（班级）+ `classroom_members`（成员，含 role 与 pending/approved 状态），隔离在应用层（API 按成员关系过滤）完成。

### 4.3 必读文章（已实现）

**设计**：教师/班委在班级内发布「必读文章」，设置起止时间；学生按时间窗口阅读。

**技术方法**：
- 数据表 `classroom_readings`（`classroom_id` + `title` + `raw_text` + `starts_at/ends_at` + `created_by`）。文章正文**直接内嵌**在阅读记录里，使必读文章成为班级的共享资源，规避了个人语料库（`materials`）RLS 按 `user_id` 隔离、学生无法读取教师材料的问题。
- 发布来源两种：**导入新文章**（直接粘贴标题 + 正文）或**从教师语料库选择**（前端读取所选材料的标题 + 原文，回填表单）。
- 权限：仅教师/班委可发布；全体成员可查看列表。
- 前端按当前时间与 `starts_at/ends_at` 比较，展示「未开始 / 进行中 / 已截止」状态。

### 4.4 阅读勾画与批注（已实现）

**设计**：学生进入必读文章后，可选中原文文字进行**词汇勾画**（高亮标记）和**句子批注**（高亮 + 注释）。

**技术方法**：
- 数据表 `reading_annotations`（`reading_id` + `user_id` + `text` + `color` + `note`），按「阅读记录 + 用户」隔离，每个学生只看到自己的勾画/批注。
- 复用自学模式语料库原文阅读的「选中文字 → 高亮/批注」交互：前端对原文做大小写不敏感的定位渲染（`renderParagraph`），将批注高亮叠回正文。
- 权限：任何班级成员均可勾画/批注；只能删除自己的批注。

### 4.5 教师划线出题（已实现）

**设计**：教师/班委进入必读文章后，可选中原文句子**添加题目**，题目以卡片形式列在原文下方，全班共享（后续学生据此作答）。

**技术方法**：
- 数据表 `reading_questions`（`reading_id` + `sentence` + `question` + `created_by`），班级共享。
- 前端在文字选中工具栏中，对教师/班委额外显示「添加题目」入口；题目关联的句子在原文中用橙色下划线标出。
- 班级仪表盘上「课后作业」卡片显示**待完成数量**（未截止的必读文章数）。

### 4.6 课后作业：作答、批改与加入资料库（已实现）

**设计**：学生阅读必读文章后，可把文章**加入个人资料库**（复用 AI 提取 + 卡片勾选），并**回答老师划线的题目**；答案存入学生档案，教师可查看作答、**批改留言**，学生端看到「教师批注」。

**技术方法**：
- 数据表 `reading_answers`（`question_id` + `user_id` + `answer` + `feedback` + `graded_at`），一名学生一题一条（`unique(question_id, user_id)`）。
- 阅读详情接口按角色返回答案：学生只看到自己的，教师/班委看到全班作答（含邮箱）。
- 「加入我的资料库」复用自学模式 `/api/materials`（AI 提取 → 生成草稿卡片 → 跳转 `corpus/review` 勾选）。
- 必读文章阅读页**不提供全文翻译**（与自学模式区分），但保留高亮与句子批注。

### 4.7 通知系统与防重复加入资料库（已实现）

**设计**：学生提交必读文章作答后，教师收到「某学生提交了《某文章》的作答」通知；教师批改后，学生收到「老师批改了你的《某文章》作答」通知。同时，学生把必读文章加入资料库后，阅读页显示「已加入资料库」，避免重复加入。

**技术方法**：
- 数据表 `notifications`（`user_id` + `type`(`submission`/`feedback`) + `classroom_id` + `reading_id` + `title` + `read`），按用户隔离。
- `src/lib/notifications.ts` 提供 `notifyTeachers`（查班级教师批量写入）与 `notifyStudent`（写给学生）。
- 通知触发点：学生作答路由（`answer/route.ts`）→ `notifyTeachers`；教师批改路由（`feedback/route.ts`）→ `notifyStudent`。
- 通知 API：`GET /api/notifications`（列表 + 未读数）、`POST /api/notifications`（全部已读）、`POST /api/notifications/[id]/read`（单条已读）。
- 前端：`Navbar` 增加铃铛 + 未读红点；`/notifications` 通知列表页，点击跳转到对应阅读页并标记已读。
- 防重复：`materials` 加 `reading_id` 列标记来源；阅读详情接口返回 `added_to_corpus`，前端据此把「加入我的资料库」按钮换成「已加入资料库」。

### 4.8 辅助备课：AI 生成教学课件（已实现）

**设计**：教师把必读文章作为备课素材——勾画的词汇/批注是**私有的备课依据**（学生不可见），划线的题目**发布给学生**。教师进入「备课模式」后，选择交付格式（PPT/Word）、题型与数量，AI 依据原文 + 教师标注生成教学课件，以可下载文件交付。

**技术方法**：
- 入口：教师视角在「布置作业」列表**鼠标悬浮**到某篇文章时显示「进入备课」按钮，跳转阅读页的 `?prep=1` 备课模式（不显示学生作答，只聚焦备课）。
- 数据复用：`reading_annotations`（教师本人勾画/批注）作为生词与教学重点；`reading_questions`（已发布题目）作为练习参考，一并传入 AI。
- AI 生成 `src/lib/lesson/content.ts`：DeepSeek 输出统一 JSON（`title`/`objectives`/`vocabulary`/`outline`/`exercises`），题型限定 `blank/choice/truefalse/qa`。
- 文件生成 `src/lib/lesson/build.ts`：`pptxgenjs` 生成 `.pptx`、`docx` 生成 `.docx`，均输出 Buffer 后以 base64 dataURL 返回，前端 `<a download>` 直接下载，免去存储与链接管理。
- 接口 `POST /api/classrooms/[id]/readings/[readingId]/lesson`：教师鉴权 → 拉取原文/批注/题目 → 校验需求 → 生成 → 返回 `{ filename, mime, dataUrl }`。
- 许可证：`pptxgenjs`/`docx` 均依赖 `jszip`（`MIT OR GPL-3.0`），已在 `scripts/check-licenses.mjs` 加白名单并注明选择 MIT 分支。

### 4.9 单词讲解 + 语言自动识别（已实现）

**设计**：备课时可勾选「需要单词讲解」，AI 为教师勾画的每个词生成**词性 + 释义 + 例句**；PPT 一词一页，Word 用字号区分（词大、释义/例句小）。同时彻底移除账户级目标语言，语言改为**按素材自动识别**。

**技术方法**：
- 单词讲解：`content.ts` 的 `vocabulary` 增加 `pos`/`example` 字段，`wordExplanation` 开关控制是否生成；`build.ts` 按开关在 PPT 逐词一页、Word 逐词分节（`TextRun` 字号/加粗/斜体区分）或维持简单生词表。
- 语言识别：`language.ts` 新增 `detectLanguage(text)`——按西语功能词 + 重音字符 vs 英语功能词打分，导入时自动判定 es/en。
- 数据层 `db/migrate_language.sql`：`materials.lang`、`classrooms.lang`、`mistake_book.lang` 三个语言列（默认 `es`）。
- 语料库：`materials` 导入时存 `lang`，翻译用 `material.lang`；口语练习/润色/错题口译按内容语言判定，错题保存时记录 `lang`。
- 教学模式：发布必读文章时检测语言并回写 `classrooms.lang`，辅助备课改用 `classrooms.lang`（不再用账户语言）。
- 移除全局语言选择：登录/注册页、导航栏的语言切换均删除；`signUp`/`setTargetLang`/`useTargetLang` 移除；主页品牌统一为 `LinguaLab`（去掉 HablaYa/SpeakUp）。

### 4.10 语言标签、导入校验与语料库分语言筛选（已实现）

**设计**：导入时若非西语/英语则拦截提示「目前仅支持英语和西班牙语，其他语种仍在开发中」；导入后在语料库（CEFR 旁）和课堂（班级名旁 / 必读文章旁）显示「英语/西语」标签；语料库按语言分隔并支持筛选。

**技术方法**：
- `language.ts` 新增 `detectSupportedLanguage(text)` → `es/en/other`：非拉丁文字（CJK/西里尔/阿拉伯等）与法语/德语等其它拉丁语的重音字符判定为 `other`。
- 导入校验：`materials` POST 与 `readings` POST 在保存前检测，`other` 直接返回 400 提示语。
- 语料库：`materials` GET 增加 `lang` 字段与 `?lang=` 过滤；`corpus` 页新增「全部/英语/西语」筛选，并在 CEFR 等级旁显示语言标签。
- 课堂：班级仪表盘（班级名旁）与「必读文章」标题旁显示语言标签（取自 `classrooms.lang`）。

### 4.11 笔头作业（已实现）

**设计**：教师发布笔头作业（标题 + 内容 + 截止时间），发布时把当时班级成员固化为「发布对象快照」；学生提交文字，教师查看提交、写评语与打分、看未交名单，学生端看到评语与分数。

**技术方法**：
- 复用队友交付的数据层 `db/migrate_assignments.sql`（`classroom_assignments` / `assignment_recipients` / `assignment_submissions`），沿用「班级共享表应用层隔离」架构。
- API：`POST/GET /api/classrooms/[id]/assignments`（发布 + 列表）、`GET .../assignments/[assignmentId]`（详情 + 提交 + 未交名单）、`POST .../assignments/[assignmentId]/submit`（学生提交 upsert）、`POST .../submissions/[submissionId]/grade`（教师批改评分）。
- 前端：`/teaching/[id]/assignments` 页「笔头作业」区——教师发布/看已交数、批改；学生提交/查看评语分数；未交名单按学生（排除教师）计算。
- 说明：当前为文字提交；图片/附件上传与 OCR 留作后续。

### 4.12 必读文章多级结构 + 班级作答分析（已实现）

**设计**：必读文章从「一页堆满」改成多级结构——二级（文章页：原文 + 题目 + 备课笔记）、三级（题目详情：已答/未答学生 + AI 班级分析）、四级（单个学生作答：教师批注）。学生端有学习引导，并在完成勾画与作答后引导「加入语料库」。

**技术方法**：
- 文章页（二级）：题目改为可点击卡片跳转三级页；教师选中原文可「发布题目 / 加入备课笔记」，学生选中可勾画/批注；学生端新增「学习引导」卡片（勾画/作答完成状态 + 加入语料库入口）。
- 题目详情（三级）：`GET .../questions/[questionId]` 返回题目 + 全部作答（教师）/ 我的作答（学生）+ 学生名单；前端按「已答/未答」分组，已答学生点击进入四级。
- 作答批注（四级）：`.../questions/[questionId]/answers/[answerId]` 页面，教师填写批改留言（复用已有 feedback 接口）。
- 班级分析：`src/lib/ai/analysis.ts` 按学生勾画词频统计「高频勾画词」，DeepSeek 分析「共性错误/同类型问题」与「讲解侧重点推荐」（只统计学生勾画，排除教师备课笔记）；接口 `POST .../questions/[questionId]/analysis`。
- 语料库联动：学生「加入语料库」复用自学模式导入（原文 → AI 提取 → review），学生在必读文章里的勾画/批注不随导入迁移，只留在阅读板块。

### 4.13 规划中的教学功能

#### 学生档案（待开发 · 明日优先）

- **指标**：按时提交率 = 按时提交作业次数 / 班级已发布作业总数。
- **教师反馈分级**：教师在批改时增加等级（A / A+ / A++），与评语、分数一起留档。
- **权限**：学生只能查看自己的档案；教师可查看全班所有学生的档案。
- **学生端展示**：进入班级后，在班级页顶部显示自己的档案卡片。
- **教师端入口**：把「班级成员」卡片改名为「班级管理」，内部包含：入班申请审批 + 全班学生档案列表。

- 教师端班级分析（个体指标 + 班级整体易错点）。
- 班级排行榜（前三名激励）。
- 笔头作业教师附件上传、点开学生提交详情（见 `WRITING_FIX_TASKS.md`，交由写作模块负责人）。

---

## 五、关键问题与解决方案

### 5.1 语音识别「只识别出一个词」

**问题**：用户说完整一句话，ASR 只返回一个词，导致对话答非所问。

**排查与解决**（逐步定位根因）：
1. 初始用浏览器 `MediaRecorder` 输出 WebM 容器，却按 `format=opus` 发送——**容器与裸流格式错配**。
2. 改为 `AudioContext` + `ScriptProcessor` 采集 16kHz PCM 并封装 WAV，解决采样率不支持（`UNSUPPORTED_SAMPLE_RATE`）。
3. 最终确认真正原因包含**麦克风未开启**（录到静音）；同时将识别从「一句话识别」升级为**实时语音识别（WebSocket 流式）**，支持整段连续转写。
4. 增加**静音检测**：录音音量过低时直接提示「未检测到声音，请检查麦克风」，避免误判。

### 5.2 多用户数据隔离

**问题**：接入多用户后，每个用户只能看到自己的数据。

**解决**：
- Supabase Auth（邮箱密码登录），前端 `apiFetch` 自动附带 JWT。
- 后端 `getUserClient` 用 JWT 换取用户身份，返回**带用户上下文的客户端**。
- 数据库启用 **RLS 行级安全**，策略 `user_id = auth.uid()`；API 层再显式按 `user_id` 过滤，双保险。

### 5.3 双语学习（西班牙语 / 英语）

**问题**：产品要支持学西语和学英语两种学习目标，界面保持中文。

**解决**：
- 引入 `TargetLang`（es/en）存于用户元数据，登录时选择、可后改。
- AI 提示词按目标语言参数化（`西班牙语/英语`），词义解释、润色说明等中文部分不变。
- TTS 发音人按语言切换（西语 Camila / 英语 Cindy）；ASR appkey 按语言选择不同 NLS 项目。
- 网站名：中性产品名 `LinguaLab`，主页面按目标语言显示 `HablaYa`（西语）/ `SpeakUp`（英语）。

### 5.4 许可证合规检测

**问题**：法务要求检测代码是否包含 GPL 等强传染性许可证。

**解决**：编写 `scripts/check-licenses.mjs` 扫描全部依赖的 license 字段，命中 GPL/AGPL/SSPL 即失败、命中 LGPL/MPL 等弱传染仅告警。扫描结果：**无强传染性许可证**；仅 3 个 MPL-2.0（构建/开发工具，文件级 copyleft，不要求整体开源）。

### 5.5 部署与国内测试

**问题**：面向国内测试组时，需要把本地开发的服务暴露到公网；此前尝试的 Vercel 方案存在两个障碍——`vercel.app` 域名国内访问不稳，且大陆手机号收不到 Vercel 风控短信。

**解决**：改用**内网穿透（cpolar）**作为当前测试部署方案：
- 本地以生产模式运行（`npm run build` + `npm run start`，端口 3100）。
- `cpolar http 3100` 把本地服务暴露为国内可访问的 HTTPS 地址，免备案、免手机验证。
- 封装成一键启动脚本 `start-hablaya.bat`，同时拉起网站与 cpolar 隧道。
- 实现「测试组 bug 报告 → 本地修复 → 重新构建 → 刷新重测」的快速闭环。

> 备选：若后续需正式对外上线，可再接入 Vercel（已预留 `vercel.json` 香港区域配置与 Node 版本锁定）。

---

## 六、技术决策记录（摘要）

| 决策 | 选择 | 理由 |
|------|------|------|
| 后端形态 | Next.js API Routes | 无需独立后端，降低复杂度 |
| 语音方案 | 阿里云 NLS 实时 WebSocket | 支持西语/英语整段连续识别 |
| 数据隔离 | Supabase RLS + 应用层过滤 | 双保险，防止越权访问 |
| AI 输出约束 | 结构化 JSON + 受控词表 | 保证可解析、标签可控 |
| 部署 | cpolar 内网穿透（国内测试） | 免备案、免手机验证、国内可达 |

---

## 七、教学模式「笔头作业」数据层（建设中）

> 本阶段只做「收发作业 + 留档」的数据底座与附件存储、OCR 骨架，前端与 API 后续实现。

### 7.1 数据模型（`db/migrate_assignments.sql`）

三张表，沿用班级共享表「应用层隔离」的既有架构（RLS 关闭，由 API 按成员关系鉴权）：

- `classroom_assignments`：作业本体（题目 + 截止时间），`created_by` 记录发布教师。
- `assignment_recipients`：**发布对象快照**——发布时把当时班级成员固化下来，避免「发布后有人退班/加人」影响未交名单的准确性。
- `assignment_submissions`：学生提交，`content`（文字）+ `media_paths`（附件 Storage 路径数组）+ `ocr_text`（图片识别结果缓存）+ 人工批改字段（`feedback`/`score`/`graded_by`）。

**解决的几个问题**：
1. 交接文档里附件字段是单 `media_url`，实际改为 `media_paths text[]`，支持一次提交多张图片/音频/视频。
2. 用 `unique(assignment_id, user_id)` 保证一人一交，重复提交走更新。
3. `assignment_submission_has_content` 约束「文字与附件至少其一」，`assignment_score_valid` 约束「0 ≤ 分数 ≤ 满分」。
4. 加 `set_updated_at()` 触发器，教师批改后自动刷新 `updated_at`。

### 7.2 附件存储（`db/migrate_storage_assignments.sql`）

新建**私有桶** `assignment-files`（`public = false`）：学生提交属于班级内私密数据，不走公开 URL。限单文件 25MB、白名单 MIME（图片/音频/视频/PDF）。两条存储策略仅允许登录用户上传/读取本桶，班级级隔离仍放在应用层（与班级共享表一致）。

### 7.3 图片提取文字（OCR，`src/lib/aliyun/ocr.ts`）

选用**阿里云 OCR**（负责人已有阿里云账号，与语音 NLS 同用一套 AccessKey，账务统一）。封装 `recognizeText(imageBase64)`，签名逻辑复用 `speech.ts` 的 RPC HMAC-SHA1 方式。**待办**：上线前到阿里云控制台核对实际开通的产品线 endpoint 与请求字段（`img`/`body`/`url`）。

### 7.4 功能接入完成（2026-09-07）

- 写作润色：完成题目、作文、可选评分标准输入，AI 语法纠错与分维度评分，建议可勾选加入错题本。
- 错题复习：增加口译/笔译入口；口译为短句并支持语音，笔译为较长复杂句，判题强化句式与选词准确性。
- 笔头作业：完成教师发布、学生文字及图片/音频/视频/PDF 提交、教师查看、未交名单、分数和反馈留档。
- 附件：复用私有桶 `assignment-files`，路径固定为 `{assignment_id}/{user_id}/{uuid}`；策略收紧为学生仅访问本人附件、教师仅访问所教班级附件。
- OCR：教师可对提交中的图片主动提取文字，结果缓存到 `assignment_submissions.ocr_text`，失败时保留原提交并返回可重试错误。
- 兼容性：保留最新版显式语言识别与仅“教师/学生”角色模型，没有修改 `src/lib/ai/practice.ts`，也没有回退已有教学功能。

验证结果：ESLint 通过、TypeScript `--noEmit` 通过、Next.js 生产构建通过；本地页面返回 200，作业列表、OCR、错题练习的未登录请求均返回 401。

---

## 八、今日开发记录（2026-09-07）

- 合并 `feature/writing-complete`：写作润色、口译/笔译错题、笔头作业媒体/OCR。解决 3 处笔头作业冲突，采用队友的组件化实现，删除我方冗余的 detail/grade 接口。
- 新增「帮助」文档（个人主页 → 帮助）+ 导航栏右上角帮助按钮；教学模式说明按教师/学生分块（同一框内）。
- 教学模式卡片改名：学生「口语练习」→「课外练习」、教师「口语练习」→「备课资料库」。
- 新建 `WRITING_FIX_TASKS.md`：记录「教师发布附件上传」「点开学生提交详情」两项待修，交由写作模块负责人。
- 规划学生档案（按时提交率 + 教师分级 A/A+/A++ + 班级管理），详见 4.12，明日开发。

### 8.1 笔头作业两项修复

- 教师发布表单新增图片、音频、视频、PDF 附件，最多 5 个、单文件不超过 25MB；发布接口改为兼容 `multipart/form-data`，附件仍使用 `assignment-files/{assignment_id}/{user_id}/{uuid}`。
- 新增 `db/migrate_assignments_attachments.sql`，为 `classroom_assignments` 增加 `media_paths`；作业列表为教师附件生成一小时有效的签名链接。
- Storage 读取策略允许教师读取所教班级的提交附件，学生只能读取本人附件和本班教师发布的作业附件，避免同学之间互看提交；上传与删除仍限定为路径中的本人 UID。
- 教师端提交列表改为“学生摘要行 → 点击展开详情”，摘要展示邮箱、提交时间、按时/迟交和评分状态，展开后才显示全文、附件、OCR 与批改表单。
- 发布过程中若附件上传、附件路径回写或发布对象快照失败，会删除已上传文件和新建作业，避免半成品记录。
