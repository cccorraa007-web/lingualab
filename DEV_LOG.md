# LinguaLab 开发日志

> 一个面向中文母语学习者的外语「输入 → 输出」训练平台。本文档记录项目从自学模式到教学模式的设计演进、采用的技术方法，以及解决的关键问题，供项目报告参考。

---

## 一、项目概述

LinguaLab 的核心价值主张：**把用户读过的材料，自动转化成一套「能说会用」的语料库，并围绕它做口语对话、写作润色、错题复习等输出训练**，解决外语学习者「会做题、不会交流」的痛点。

产品分两大模式：

- **自学模式**：面向个人自学者，包含语料库、口语练习、写作润色、错题本。
- **教学模式**：面向真实课堂，教师 + 学生双角色、班级管理、必读文章与口语训练（逐步建设中）。

学习目标语言支持**西班牙语**和**英语**，界面为中文。

---

## 二、技术架构与技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Next.js 16（App Router）+ React 19 + Tailwind CSS | 响应式 SPA |
| 后端 | Next.js API Routes（Serverless 函数） | 省去独立后端 |
| 数据库 | Supabase（云 PostgreSQL）+ Auth 认证 + RLS 行级安全 | 一体化 |
| 大模型 | DeepSeek（deepseek-chat） | 语料分析、对话、润色、评分 |
| 语音 | 阿里云智能语音交互 NLS | ASR 实时语音识别 + TTS 语音合成 |
| 部署 | Vercel / 内网穿透（cpolar） | 生产部署 / 国内测试 |

**数据流**：前端（客户端组件）通过 `/api/*` 路由调用后端；后端校验 JWT 身份后访问 Supabase 与 AI/语音服务。

---

## 三、自学模式设计

### 3.1 语料库（数据底座）

**设计**：用户粘贴一篇外语文章，AI 自动完成「话题分类 → 提取关键词/地道表达/口语题 → CEFR 难度标注」，产出语料卡片。

**技术方法**：
- 用 `processCorpus` 以结构化 JSON（`chatJSON` + `response_format: json_object`）约束大模型输出，保证结果可解析。
- 话题标签用**受控词表**（`topics.ts` 中 30 个主题）约束，并做 `resolveTopicSlug` 归一化：AI 无论输出 slug、中文名还是外语名，都归一为合法 slug，前端再映射中文显示（解决「标签出现外语」问题）。
- 语料条目分三类：`keyword`（关键词）、`expression`（地道表达）、`prompt`（口语练习问题）。
- 支持原文高亮、句子批注（`annotations` 表）、全文翻译（`translateText`）。

### 3.2 口语练习

**设计**：分「自由练习」和「考题模式」。
- 自由练习：AI 扮演考官，基于用户语料库内容由浅入深对话引导开口。
- 考题模式：参考 SIELE 题型（简单问答/情景对话/观点表达）随机抽题、限时作答。

**技术方法**：
- 语音采用**实时语音识别（WebSocket 流式 ASR）**：前端录音经 `AudioContext` 采集成 16kHz PCM，通过 WebSocket 直连阿里云 NLS，边说话边出字，结束后拿到完整转写。
- TTS 合成考官提问语音，实现「听题 → 说话 → 评价」的闭环。
- 对话结束调用 `polishAnswers` 生成逐条润色建议（原文 → 修改 → 中文原因 → 参考例句 → 错误类型）。

### 3.3 写作润色

**设计**：把用户写的外语改「地道」，每条修改给出原因、错误类型、参考例句，并支持一键加入错题本。润色建议按场景（正式/日常）切换语气。

### 3.4 错题本

**设计**：把润色/练习中发现的错误沉淀为错题，配合「口译练习」复习：AI 出中文提示 → 学生口译成外语 → AI 判定是否正确、连续答对自动移除。

---

## 四、教学模式设计（建设中）

### 4.1 角色与权限

- 账号本身**不区分身份**（注册时不选教师/学生，自学模式对两者一致）。
- 身份在**创建/加入班级时自行选择**：创建班级时选「以教师身份/以学生身份」，加入班级时同样选「以教师身份/以学生身份」——学生也可以建班，教师通过加入时选教师身份进入，支持一名以上教师。
- 班级内角色分三档：`teacher`（教师）、`leader`（班委，由教师指定）、`student`（普通学生）；教师可把普通学生设为班委或教师。

### 4.2 班级与成员管理

- **建班**：任何人可创建班级（并选择自己的身份），系统生成 6 位邀请码。
- **加入**：任何人凭邀请码申请加入，加入时选择「以教师身份/以学生身份」。
- **审批**：教师/班委审批申请。
- **班委/教师**：教师可指定普通学生为班委，或设为教师（实现多教师）。

**数据模型**：`classrooms`（班级）+ `classroom_members`（成员，含 role 与 pending/approved 状态），隔离在应用层（API 按成员关系过滤）完成。

### 4.3 必读文章（已实现）

**设计**：教师/班委在班级内发布「必读文章」，设置起止时间；学生按时间窗口阅读。

**技术方法**：
- 数据表 `classroom_readings`（`classroom_id` + `title` + `raw_text` + `starts_at/ends_at` + `created_by`）。文章正文**直接内嵌**在阅读记录里，使必读文章成为班级的共享资源，规避了个人语料库（`materials`）RLS 按 `user_id` 隔离、学生无法读取教师材料的问题。
- 发布来源两种：**导入新文章**（直接粘贴标题 + 正文）或**从教师语料库选择**（前端读取所选材料的标题 + 原文，回填表单）。
- 权限：仅教师/班委可发布；全体成员可查看列表。
- 前端按当前时间与 `starts_at/ends_at` 比较，展示「未开始 / 进行中 / 已截止」状态。

### 4.4 阅读勾画与批注（已实现）

**设计**：学生进入必读文章后，可选中原文文字进行**词汇勾画**（高亮标记）和**句子批注**（高亮 + 注释）。

**技术方法**：
- 数据表 `reading_annotations`（`reading_id` + `user_id` + `text` + `color` + `note`），按「阅读记录 + 用户」隔离，每个学生只看到自己的勾画/批注。
- 复用自学模式语料库原文阅读的「选中文字 → 高亮/批注」交互：前端对原文做大小写不敏感的定位渲染（`renderParagraph`），将批注高亮叠回正文。
- 权限：任何班级成员均可勾画/批注；只能删除自己的批注。

### 4.5 规划中的教学功能

- AI 根据勾画/批注/主旨生成口语问题（部分教师提供）。
- 学生档案（阅读时长、生词、答题情况、易错点），下次出题参考档案。
- 教师端班级分析（个体指标 + 班级整体易错点）。
- 班级排行榜（前三名激励）。

---

## 五、关键问题与解决方案

### 5.1 语音识别「只识别出一个词」

**问题**：用户说完整一句话，ASR 只返回一个词，导致对话答非所问。

**排查与解决**（逐步定位根因）：
1. 初始用浏览器 `MediaRecorder` 输出 WebM 容器，却按 `format=opus` 发送——**容器与裸流格式错配**。
2. 改为 `AudioContext` + `ScriptProcessor` 采集 16kHz PCM 并封装 WAV，解决采样率不支持（`UNSUPPORTED_SAMPLE_RATE`）。
3. 最终确认真正原因包含**麦克风未开启**（录到静音）；同时将识别从「一句话识别」升级为**实时语音识别（WebSocket 流式）**，支持整段连续转写。
4. 增加**静音检测**：录音音量过低时直接提示「未检测到声音，请检查麦克风」，避免误判。

### 5.2 多用户数据隔离

**问题**：接入多用户后，每个用户只能看到自己的数据。

**解决**：
- Supabase Auth（邮箱密码登录），前端 `apiFetch` 自动附带 JWT。
- 后端 `getUserClient` 用 JWT 换取用户身份，返回**带用户上下文的客户端**。
- 数据库启用 **RLS 行级安全**，策略 `user_id = auth.uid()`；API 层再显式按 `user_id` 过滤，双保险。

### 5.3 双语学习（西班牙语 / 英语）

**问题**：产品要支持学西语和学英语两种学习目标，界面保持中文。

**解决**：
- 引入 `TargetLang`（es/en）存于用户元数据，登录时选择、可后改。
- AI 提示词按目标语言参数化（`西班牙语/英语`），词义解释、润色说明等中文部分不变。
- TTS 发音人按语言切换（西语 Camila / 英语 Emily）；ASR appkey 按语言选择不同 NLS 项目。
- 网站名：中性产品名 `LinguaLab`，主页面按目标语言显示 `HablaYa`（西语）/ `SpeakUp`（英语）。

### 5.4 许可证合规检测

**问题**：法务要求检测代码是否包含 GPL 等强传染性许可证。

**解决**：编写 `scripts/check-licenses.mjs` 扫描全部依赖的 license 字段，命中 GPL/AGPL/SSPL 即失败、命中 LGPL/MPL 等弱传染仅告警。扫描结果：**无强传染性许可证**；仅 3 个 MPL-2.0（构建/开发工具，文件级 copyleft，不要求整体开源）。

### 5.5 部署与国内测试

**问题**：面向国内测试组时，需要把本地开发的服务暴露到公网；此前尝试的 Vercel 方案存在两个障碍——`vercel.app` 域名国内访问不稳，且大陆手机号收不到 Vercel 风控短信。

**解决**：改用**内网穿透（cpolar）**作为当前测试部署方案：
- 本地以生产模式运行（`npm run build` + `npm run start`，端口 3100）。
- `cpolar http 3100` 把本地服务暴露为国内可访问的 HTTPS 地址，免备案、免手机验证。
- 封装成一键启动脚本 `start-hablaya.bat`，同时拉起网站与 cpolar 隧道。
- 实现「测试组 bug 报告 → 本地修复 → 重新构建 → 刷新重测」的快速闭环。

> 备选：若后续需正式对外上线，可再接入 Vercel（已预留 `vercel.json` 香港区域配置与 Node 版本锁定）。

---

## 六、技术决策记录（摘要）

| 决策 | 选择 | 理由 |
|------|------|------|
| 后端形态 | Next.js API Routes | 无需独立后端，降低复杂度 |
| 语音方案 | 阿里云 NLS 实时 WebSocket | 支持西语/英语整段连续识别 |
| 数据隔离 | Supabase RLS + 应用层过滤 | 双保险，防止越权访问 |
| AI 输出约束 | 结构化 JSON + 受控词表 | 保证可解析、标签可控 |
| 部署 | cpolar 内网穿透（国内测试） | 免备案、免手机验证、国内可达 |
